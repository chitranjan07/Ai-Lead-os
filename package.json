{
  "name": "ai-lead-followup-os",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "install:all": "npm install && npm install --workspace server && npm install --workspace client",
    "seed": "npm run seed --workspace server",
    "test:evaluation": "npm run evaluate --workspace server",
    "build": "npm run build --workspace client",
    "azure:smoke": "npm run azure:smoke --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
